# Second Step — second-wave experiment outputs

- **`results/`** — `collect_results.sh` copies (`results_all.csv`, `arm_*` trees, optional plots from the standard analyzer).
- **`analysis/`** — extended tables and figures from `Rscript scripts/analyze_second_step_detail.R` (see `session_log.txt`).

The **`First Step/`** folder is a frozen *code* snapshot from wave one. This folder holds *wave-two result files* only.
